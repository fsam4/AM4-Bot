extends airport.pug

| <b>Total pax:</b> #{airport.totalPax.toLocaleString(locale)}
| <b>Total cargo:</b> #{airport.totalCargo.toLocaleString(locale)} lbs 
|
|
b <u>Average demand</u>
|
| <b>Economy:</b> #{airport.average_demand.Y.toLocaleString(locale)}
| <b>Business:</b> #{airport.average_demand.J.toLocaleString(locale)}
| <b>First:</b> #{airport.average_demand.F.toLocaleString(locale)}
| <b>Large:</b> #{airport.average_demand.L.toLocaleString(locale)}
| <b>Heavy:</b> #{airport.average_demand.H.toLocaleString(locale)}